import { FocusSectionDirective } from './focus-section.directive';

describe('FocusSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusSectionDirective();
    expect(directive).toBeTruthy();
  });
});
